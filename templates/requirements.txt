Flask==2.3.2               # Web framework for the backend
numpy==1.23.5              # For numerical computations
tensorflow==2.12.0         # For building and running ML models (recommendation model)
scikit-learn==1.2.2        # For training price prediction model (RandomForestRegressor)
joblib==1.2.0              # For saving and loading scikit-learn models
sqlite3                    # Built-in library for database management
requests==2.31.0           # For making HTTP requests (if integrating APIs)